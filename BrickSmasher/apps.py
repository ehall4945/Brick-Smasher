from django.apps import AppConfig


class BricksmasherConfig(AppConfig):
    # Sets the default primary key type for this app
    default_auto_field = "django.db.models.BigAutoField"
    name = "BrickSmasher"
